import { TransformerInterceptorInterceptor } from './transformer-interceptor.interceptor';

describe('TransformerInterceptorInterceptor', () => {
  it('should be defined', () => {
    expect(new TransformerInterceptorInterceptor()).toBeDefined();
  });
});
